# Lasso Import UI — Environment Variables
# Copy this file to .env and fill in your values.

# Optional: override the app base URL (set automatically in CI for GitHub Pages)
# NUXT_APP_BASE_URL=/ShowMastersLassoIntegration/

# All Lasso credentials are entered at runtime in the browser UI and stored in localStorage.
# No server-side secrets are needed.
